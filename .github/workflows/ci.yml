name: CI

on:
  push:
    branches:
      - stable
      - dev
  pull_request:

permissions:
  contents: read

jobs:
  test:
    name: Check, test and build
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup V
        uses: ./.github/actions/setup-v

      - name: Check
        run: v -old-compiler -check .

      - name: Test
        run: v -old-compiler test .

      - name: Build
        run: v -old-compiler -o vedrock .
